import React from "react";
import { DocsThemeConfig } from "nextra-theme-docs";

const config: DocsThemeConfig = {
  logo: <h1 className="text-4xl font-bold text-heading">ChatDB</h1>,
};
export default config;
